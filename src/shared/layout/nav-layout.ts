// Shared between SiteNav and SiteFrame so the nav's own content width and the root grid it sits on never drift apart, one is the source of truth for both.
export const NAV_SCROLL_OFFSET = 24;
export const NAV_CONTAINER_CLASSNAME = "px-6 sm:px-10 lg:px-32";
export const NAV_MAX_WIDTH_CLASSNAME = "max-w-6xl";
