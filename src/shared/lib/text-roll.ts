// Shared slot-text (textmotion.dev) roll tuning, reused everywhere text morphs so nothing drifts out of sync. See STANDARDS.md for how these were tuned.
export const TEXT_ROLL_OPTIONS = { duration: 220, stagger: 30, exitOffset: 20, rollBy: "character" } as const;
